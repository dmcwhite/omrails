source 'https://rubygems.org'

ruby '2.0.0'

gem 'rails', '4.0.0'
gem 'jquery-rails'
gem 'devise'
gem 'simple_form'
gem 'newrelic_rpm'

group :development, :test do
	gem 'sqlite3'
end

group :production do
	gem 'pg'
end

group :assets do
	gem 'sass-rails'
	gem 'coffee-rails'
	gem 'uglifier'
	gem 'bootstrap-sass'
end
